# Work It — מסמך העברה

מסמך ההקשר וההחלטות של הפרויקט. מיועד לסשן חדש שמתחיל בלי היסטוריה.

| | |
|---|---|
| **מוצר** | **Work It** — יומן עבודה לצוותים קטנים |
| **ריפו** | `rotemshaer-hash/tripel-pay`, ענף `main` |
| **חי באוויר** | https://triplepayil.netlify.app |
| **Firebase** | פרויקט `triplepay-prod` (Auth + Realtime Database + Storage) |
| **סטאק** | React 19 + TypeScript + Vite 8, `useReducer` גלובלי אחד |

> ⚠️ ה-`CLAUDE.md` שב-`/home/user/Telegram-rs` שייך לפרויקט אחר (Drushe/kidemy). לא רלוונטי כאן.

---

## 1. מה המוצר, ומה הוא לא

**Work It הוא תוסף ארגוני לוואטסאפ, לא אפליקציה מתחרה.** המנהל ממשיך לנהל בוואטסאפ; אנחנו מוסיפים לו זיכרון: מי קיבל, מתי ראה, מה עשה, ותמונות עם שעה.

**הבידול מול השוק** (Connecteam, Deputy, Homebase, Meckano, TimeClock365, Jobber): כולם, בלי יוצא דופן, מתחילים מ"כל עובד מתקין אפליקציה ופותח חשבון". **אצלנו העובד לא מתקין כלום ולא זוכר סיסמה — הוא לוחץ על קישור.** זה החסם היחיד שמפיל אימוץ בעסק של שלושה אנשים, וזה מה שאנחנו לא מקבלים.

**המשפט:** וואטסאפ נשאר איך שאתה מנהל. אנחנו רק מוסיפים לו זיכרון.

---

## 2. אילוצי-על — לא לשנות בלי אישור מפורש

1. **אין תשלומים אמיתיים באפליקציה.** אין סליקה, אין העברת כספים. הוראה קבועה מהמשתמש.
2. **אין שליחה אוטומטית בוואטסאפ.** נבדק והוחלט: לא נשלב את WhatsApp Cloud API של מטא. הוא דורש חשבון עסקי מאומת, מספר ייעודי, תבניות שמטא מאשרת ושרת שמאזין — והוא בתשלום (~$0.004 להודעת utility, ומ-1.10.2026 גם ההודעות שהיום חינם בתוך חלון 24 שעות יחויבו). **הסבב הידני הוא התכנון, לא מגבלה.** התשלום היה קונה נוחות למנהל, לא יכולת חדשה — התיעוד עובד בלעדיו.
3. **פרויקט Firebase `triplepay-prod` לא משנים.** השם מקדים את המיתוג ואינו טעות.

---

## 3. חסום על המשתמש — בלי זה הקישורים לא עובדים בייצור

1. **Firebase Console ← Authentication ← Sign-in method ← Anonymous ← Enable**
   דף העובד נכנס אנונימית כדי לקרוא את המשימה ולדווח. בלי זה הוא ייכשל.
2. **Firebase Console ← Realtime Database ← Rules** — להדביק את `database.rules.json` מהריפו (`taskLinks`, `linkInbox`, `workerLinks`).
3. **GitHub ← Settings ← Secrets ← `FIREBASE_SERVICE_ACCOUNT`** — כדי שהחוקים יתפרסמו אוטומטית. הוורקפלואו `deploy-firebase-rules.yml` נכשל בכל הרצה מאז ומעולם בגלל הסוד החסר.

---

## 4. איך עובדים

- **עברית, לשון זכר.** דיווח קצר אחרי כל שינוי + צילומי מסך.
- **לדחוף אוטומטית** בלי לשאול אישור בכל פעם.
- דיפלוי: דחיפה ל-`main` → GitHub Actions מריץ `CI` + `Deploy to Netlify`.
- הרשת בסביבת הפיתוח חסומה ל-Netlify/Firebase ישירות. סטטוס נבדק דרך `mcp__github__actions_list` (הפלט ענק — לחתוך את הקובץ השמור).

### לפני כל commit
`npx tsc --noEmit -p tsconfig.app.json` → `npm run build` → `npx oxlint` → `npm run check:db-boundary`.
**12 warnings זה הבסיס התקין.** יותר מזה = משהו חדש נשבר.

### בדיקה מלאה מול Firebase אמיתי (`npm run e2e:emulated`)
`e2e/smoke.mjs` מריץ את כל המוצר מול Firebase Emulator Suite — **כולל קבצי החוקים האמיתיים**. 21 בדיקות: פתיחת חשבון עסק, כתיבת משימה עם שדה אופציונלי ריק, קוד הזמנה, הרשמת עובד, אישור קבלה, העלאת קובץ, דיווח מקישור בלי חשבון, סבב יומי, התנתקות והתחברות מחדש.
**כל באג שעלה לנו ימים היה נתפס כאן** — קוד הזמנה שלא פורסם, כתיבה שנדחתה בגלל `undefined`, עובד שנרשם ולא הצליח להיכנס, העלאה בלי הרשאה. רץ ב-CI על כל דחיפה.

### בדיקה ויזואלית
1. להוסיף **זמנית** ב-`src/data/store.tsx`, בשורה הראשונה של ה-`useEffect` של האימות:
   `if ((window as unknown as { __PREVIEW__?: boolean }).__PREVIEW__) return;`
2. `npm run dev -- --port 5183`, Playwright עם `/opt/pw-browsers/chromium`, זריעת `localStorage` במפתח `triple-pay-state-v1` **רק אם ריק** (זריעה עיוורת דורסת את מה שהאפליקציה כתבה וגורמת לפיצ'ר תקין להיראות שבור — זו טעות שחזרה פעמיים).
3. **תמיד להסיר את ה-guard לפני commit** (`grep -c "__PREVIEW__" src/data/store.tsx` = 0) ולמחוק את הסקריפט.

---

## 5. ארכיטקטורה — מה שחייבים לדעת

- **`src/data/store.tsx`** — reducer יחיד, כל הפעולות. `mapChild` / `mapTask` לעדכון ממוקד.
- **`src/data/vocabulary.ts`** — `MODE` מחליף בין `"work"` ל-`"family"`. אותה בסיס-קוד, שני מוצרים. כל מילה וכל צבע מוגדרים שם פעם אחת. **קוד המשפחה כבר לא נשלח לדפדפן בגרסה העסקית** — הבנדלר מוציא אותו; המחיר היחיד הוא תחזוקה.
- **מודל הנתונים עדיין בשמות המשפחתיים** (`child` = עובד, `parent` = מנהל, `family` = עסק). שכתוב שווה יום עבודה — פתוח, לא דחוף.
- **`role` מול `viewMode`** — מי אתה מול איזה צד אתה מסתכל עליו. `useWorkView()` הוא ההגדרה היחידה. `viewMode` **לא נשמר** בין טעינות: כשמור, מנהל שלחץ פעם על תצוגת עובד נשאר שם לנצח וכל הכפתורים שלו נעלמו.
- **גבול גישה ל-Firebase** — רק `data/store.tsx` מייבא מ-`src/firebase/*`, גם לא `import type`. `scripts/check-db-boundary.mjs` מפיל את ה-CI אחרת.
- **Firebase דוחה `undefined`** — ודוחה את **כל הכתיבה**, לא את השדה. הניקוי נעשה בגבול אחד, ב-`forFirebase()` שב-`firebase/db.ts`.
- **Firebase מחזיר מערך ריק כ"חסר"** — `normalizeFamily`/`normalizeChild` ממלאים ברירות מחדל. לכל שדה מערך חדש חובה להוסיף שם שורה.
- **כשל שקט אסור.** כל קריאה וכתיבה מדווחת על עצמה: `connection` בסטור מחזיק מי מחובר, אם הנתונים מהשרת או מהמכשיר, ומה נכשל. ההגדרות מציגות את זה. כל "לא עובד" שעלה ימים היה מצב נסתר שהאפליקציה ידעה ולא אמרה.

### הקישורים — לב המוצר
| נתיב | מה |
|---|---|
| `/d/{dayToken}` | **הקישור היומי של העובד** — כל המשימות הפתוחות שלו, בלי חשבון. זה הקישור שנשלח בבוקר. |
| `/w/{linkToken}` | קישור למשימה בודדת (נשאר לשליחה נקודתית). |
| `taskLinks/{token}`, `workerLinks/{token}` | **תצלום** של משימה/יום — לעולם לא דלת לרשומת העסק. מתפרסם מחדש כשהעבודה משתנה. |
| `linkInbox/{familyUid}` | לאן הדיווחים נוחתים. מאזין אחד לכל עסק; המנהל מחיל ומוחק, כך שדיווח לא נכנס פעמיים. |

הטוקן **הוא** ההרשאה — אותה עסקה כמו קישור הורדה של Firebase. דיווח שנכשל נשמר ב-`localStorage` של העובד ונשלח לבד כשהרשת חוזרת.

---

## 6. מה פתוח

- שליחה **במייל** (הקישור עובד ממייל; השליחה האוטומטית לא בנויה).
- התראות Push.
- שכתוב שמות המודל (`child` → עובד) — חוב תחזוקה, לא באג.
- מסך הספקים/מסמכים נשאר מינימלי בכוונה עד שיהיו עסקים משתמשים.
- **סגור ולא נפתח מחדש:** שליחה אוטומטית בוואטסאפ (סעיף 2.2).
