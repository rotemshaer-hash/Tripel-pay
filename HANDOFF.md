# Work It — מסמך העברה

מסמך ההקשר וההחלטות של הפרויקט. מיועד לסשן חדש שמתחיל בלי היסטוריה — **לקרוא אותו במלואו לפני שנוגעים בקוד.**

| | |
|---|---|
| **מוצר** | **Work It** — יומן עבודה לצוותים קטנים |
| **ריפו** | `rotemshaer-hash/tripel-pay`, ענף `main` (התיקייה בסשן: `/home/user/tripel-pay`) |
| **חי באוויר** | https://triplepayil.netlify.app (Netlify site `triplepayil`, id `907bf8a0-4060-4ce8-baa2-6133c7b32e61`) |
| **Firebase** | פרויקט `triplepay-prod` — Auth + Realtime Database + Storage |
| **סטאק** | React 19 + TypeScript + Vite 8, `useReducer` גלובלי אחד, בלי שרת |

> ⚠️ ה-`CLAUDE.md` שב-`/home/user/Telegram-rs` שייך לפרויקט אחר (Drushe/kidemy). לא רלוונטי כאן.

---

## 1. מה המוצר, ומה הוא לא

**Work It הוא תוסף ארגוני לוואטסאפ, לא אפליקציה מתחרה.** המנהל ממשיך לנהל בוואטסאפ; אנחנו מוסיפים לו זיכרון: מי קיבל, מתי ראה, מה עשה, ותמונות עם שעה.

**הבידול** (מול Connecteam, Deputy, Homebase, Meckano, TimeClock365, Jobber, Workiz): כולם מתחילים מ"כל עובד מתקין אפליקציה ופותח חשבון". **אצלנו העובד לא מתקין כלום ולא זוכר סיסמה — הוא לוחץ על קישור.** נבדק בחיפוש רשת ולא נמצא מתחרה ישיר; הפער הוא בהרכבה, לא בטכנולוגיה. המתחרים לא יוותרו על חשבון־פר־עובד כי המודל העסקי שלהם בנוי עליו.

**המשפט:** וואטסאפ נשאר איך שאתה מנהל. אנחנו רק מוסיפים לו זיכרון.

---

## 2. אילוצי-על — לא לשנות בלי אישור מפורש

1. **אין תשלומים אמיתיים באפליקציה.** אין סליקה, אין העברת כספים. הוראה קבועה מהמשתמש.
2. **אין שליחה אוטומטית בוואטסאפ.** נבדק והוחלט: לא משלבים את WhatsApp Cloud API של מטא. הוא דורש חשבון עסקי מאומת, מספר ייעודי, תבניות שמטא מאשרת ושרת שמאזין — והוא בתשלום (~$0.004 להודעת utility; מ-1.10.2026 גם ההודעות שהיום חינם בתוך חלון 24 שעות יחויבו). **השליחה הידנית היא התכנון, לא מגבלה.**
3. **פרויקט Firebase `triplepay-prod` לא משנים.** השם מקדים את המיתוג ואינו טעות.
4. **הטופס נשאר בן חמישה שדות.** מה צריך לעשות · פירוט · עובד · תאריך יעד · לקוח. תבניות מקצוע, שלבים, עדיפות ותדירות הוסרו ממנו במפורש לבקשת המשתמש ("הכי פשוט שיש").
5. **עברית, לשון זכר.** דיווח קצר אחרי כל שינוי + צילומי מסך. **דוחפים אוטומטית** בלי לבקש אישור בכל פעם.

---

## 3. חסום על המשתמש — בלי זה הקישורים לא עובדים בייצור

זה **הדבר הפתוח הכי חשוב**. שלושה מתגים חיים בקונסולה של Firebase ולא בריפו:

1. **Authentication ← Sign-in method ← Anonymous ← Enable** — דף העובד נכנס אנונימית כדי לקרוא את המשימה ולדווח.
2. **Realtime Database ← Rules** — להדביק את `database.rules.json` מהריפו (`taskLinks`, `linkInbox`, `workerLinks`).
3. **Storage ← Rules** — להדביק את `storage.rules` (וש-Storage מופעל בכלל).

**באפליקציה יש בדיקה שאומרת מי מהם חסם:** הגדרות ← **"בדיקת מוכנות"** ← "הרצת בדיקה". היא מריצה בדיוק את מה שעושה הטלפון של העובד (כניסה אנונימית על אינסטנס Firebase נפרד כדי לא לנתק את המנהל, קריאת `workerLinks`, והעלאת קובץ בדיקה ל-Storage) ומדפיסה לכל כשל את שורת התיקון המדויקת. **אם המשתמש מדווח ש"לא עובד" — לבקש קודם צילום של הבדיקה הזו.**

בנוסף: הסוד `FIREBASE_SERVICE_ACCOUNT` ב-GitHub לא הוגדר, ולכן `deploy-firebase-rules.yml` נכשל בכל הרצה מאז ומעולם, והחוקים מתפרסמים רק ידנית.

---

## 4. איך עובדים

### דיפלוי
דחיפה ל-`main` → GitHub Actions מריץ `CI` + `Deploy to Netlify`. הרשת בסביבת הפיתוח **חסומה** ל-Netlify ול-Firebase, אז סטטוס נבדק דרך `mcp__github__actions_list` (הפלט ענק — לחתוך את הקובץ השמור עם `head -c`).

### לפני כל commit
`npx tsc --noEmit -p tsconfig.app.json` → `npm run build` → `npx oxlint` → `npm run check:db-boundary`.
**12 warnings זה הבסיס התקין.** יותר = משהו חדש נשבר.

### הבדיקה שתופסת באגים אמיתיים — `npm run e2e:emulated`
`e2e/smoke.mjs` מריץ את כל המוצר מול Firebase Emulator Suite, **כולל קבצי החוקים האמיתיים**. 31 בדיקות: פתיחת חשבון, כתיבת משימה עם שדה אופציונלי ריק, שליחה מאותו מסך, קוד הזמנה, הרשמת עובד, אישור קבלה, העלאת קובץ, דיווח מקישור בלי חשבון, שער האסמכתאות, עריכה והערה שמגיעות לעובד, התנתקות והתחברות.

הרצה מקומית:
```bash
npm i -g firebase-tools@13                     # פעם אחת בסביבה חדשה
VITE_USE_FIREBASE_EMULATOR=true npm run build
npm run preview -- --port 4173 &
E2E_CHROMIUM=/opt/pw-browsers/chromium firebase emulators:exec \
  --only auth,database,storage --project triplepay-test "node e2e/smoke.mjs"
```
**כל באג שעלה לנו ימים היה נתפס כאן** — קוד הזמנה שלא פורסם, כתיבה שנדחתה בגלל `undefined`, עובד שנרשם ולא הצליח להיכנס, העלאה בלי הרשאה.

### צילומי מסך
אותו הרכב בדיוק (אמולטור + preview על 4173), עם סקריפט Playwright חד-פעמי שנמחק אחרי השימוש. אם צריך לעקוף את Firebase לגמרי: guard **זמני** בשורה הראשונה של ה-`useEffect` של האימות ב-`store.tsx` (`if ((window as unknown as { __PREVIEW__?: boolean }).__PREVIEW__) return;`), זריעת `localStorage` במפתח `triple-pay-state-v1` **רק אם ריק**, ו**להסיר את ה-guard לפני commit** (`grep -c "__PREVIEW__" src/data/store.tsx` = 0).

---

## 5. המסכים

| נתיב | מי | מה |
|---|---|---|
| `/login` | שניהם | כניסה: מנהל (מייל) או עובד (שם משתמש). כולל מסלול תיקון לחשבון שנפתח חצי |
| `/work/board` | מנהל | **הבית.** כפתור "משימה חדשה" + רשימה: פתוחות / הושלמו, תגית מצב לכל משימה, אישור בלחיצה |
| `/work/new` | מנהל | חמישה שדות, ואחרי "הקצאה" — פאנל שליחה באותו מסך |
| `/work/task/:workerId/:taskId` | שניהם | פירוט מלא: הנחיות, אסמכתאות, הערות, יומן פעילות, עריכה |
| `/work/journal` | מנהל | יומן הפעילות המלא + ייצוא CSV ודוח |
| `/work/report` | מנהל | דוח PDF (הדפסה) + **תיק הוכחות ללקוח** לפי אתר ותקופה |
| `/work/team` | מנהל | צוות, טלפונים, קודי הזמנה, שליחת המשימות, הנפקת קוד חדש |
| `/work/directory` | שניהם | ספקים ומסמכים (מינימלי בכוונה) |
| `/work/settings` | שניהם | חשבון, בדיקת מוכנות, אסמכתאות חובה, מנהל נוסף, התקנה, מחיקת חשבון |
| `/d/:token` | עובד | **הקישור היומי** — כל המשימות הפתוחות שלו, בלי חשבון |
| `/w/:token` | עובד | קישור למשימה בודדת |
| `/join?code=` | עובד | הרשמה עם קוד (למי שרוצה חשבון מלא) |

---

## 6. ארכיטקטורה — מה שחייבים לדעת

- **`src/data/store.tsx`** — reducer יחיד, כל הפעולות. `mapChild` / `mapTask` לעדכון ממוקד.
- **`src/data/vocabulary.ts`** — `MODE` מחליף בין `"work"` ל-`"family"`. אותה בסיס-קוד, שני מוצרים; כל מילה וכל צבע מוגדרים שם פעם אחת. קוד המשפחה כבר לא נשלח לדפדפן בגרסה העסקית (הבנדלר מוציא אותו).
- **מודל הנתונים עדיין בשמות המשפחתיים** — `child` = עובד, `parent` = מנהל, `family` = עסק. חוב תחזוקה מוכר, לא באג.
- **`role` מול `viewMode`** — מי אתה מול איזה צד אתה רואה. `useWorkView()` היא ההגדרה היחידה. `viewMode` **לא נשמר** בין טעינות.
- **גבול Firebase** — רק `data/store.tsx` מייבא מ-`src/firebase/*`, גם לא `import type`. `scripts/check-db-boundary.mjs` מפיל את ה-CI אחרת.
- **Firebase דוחה `undefined`** ודוחה את **כל הכתיבה**. הניקוי בגבול אחד: `forFirebase()` ב-`firebase/db.ts`.
- **Firebase מחזיר מערך ריק כ"חסר"** — `normalizeFamily`/`normalizeChild` ממלאים. לכל שדה מערך חדש חובה להוסיף שם שורה.
- **כשל שקט אסור.** `connection` בסטור מחזיק מי מחובר, אם הנתונים מהשרת או מהמכשיר, ומה נכשל; ההגדרות מציגות את זה, ויש כפתור סנכרון ידני. כל "לא עובד" שעלה ימים היה מצב נסתר שהאפליקציה ידעה ולא אמרה.
- **עיצוב** — שכבת "זכוכית" מוגדרת פעם אחת ב-`src/index.css`: `.work-ground` (רקע צבע), `.pane` / `.pane-tint` (כרטיסים עם הבהק), `.hero`, `.glass-bar`, `.pill`, `.press`. **אין לערבב צבעים ידנית בתוך קומפוננטה.**

### הקישורים — לב המוצר
| נתיב ב-DB | מה |
|---|---|
| `workerLinks/{dayToken}` | **תצלום** של כל המשימות הפתוחות של עובד. מתפרסם מחדש כשהעבודה משתנה |
| `taskLinks/{linkToken}` | תצלום של משימה בודדת |
| `linkInbox/{familyUid}` | לאן הדיווחים נוחתים. מאזין אחד לכל עסק; המנהל מחיל ומוחק, כך שדיווח לא נכנס פעמיים |

הטוקן **הוא** ההרשאה — אותה עסקה כמו קישור הורדה של Firebase. התצלום לעולם אינו דלת לרשומת העסק. דיווח שנכשל נשמר ב-`localStorage` של העובד ונשלח לבד כשהרשת חוזרת.

### שליחה
- **לאדם** — `wa.me` עם המספר השמור פותח את הצ'אט ישירות. בלי מספר: בורר אנשי קשר. יש "+ מספר" ליד כפתור השליחה שמכניס אותו במקום.
- **לקבוצה** — `wa.me` **לא יכול** לפתוח קבוצה (לקבוצה אין מספר). הפתרון: `navigator.share` (חלון השיתוף של הטלפון), ובמחשב — העתקה. הודעת הקבוצה **בלי קישור אישי**, כי קישור הוא זהות של אדם אחד.
- **כמה עובדים על משימה** — כל אחד מקבל **עותק משלו** עם `crewId` משותף. רשומה משותפת אחת הייתה עונה "מי עשה מה" ב"מישהו".

### צירוף קבצים
`AttachButton` — מצלמה / גלריה / קובץ (**בלי `accept`**, אחרת אנדרואיד פותח בורר מצומצם בלי Drive) / **קישור** (Google Sheets ו-Docs אינם קבצים על המכשיר; קישור הוא גם התשובה הנכונה כי עותק מתיישן). תמונות נשמרות כ-data URL בתוך הרשומה; קבצים אמיתיים עולים ל-Storage.

---

## 7. מה פתוח

- **שלושת המתגים בקונסולה (סעיף 3)** — החסם היחיד שמונע שימוש אמיתי.
- שליחה **במייל** (הקישור עובד ממייל; השליחה האוטומטית לא בנויה).
- התראות Push.
- **משימות קבועות** — המנוע קיים (`ROLL_RECURRING_TASKS`) אבל אין שדה תדירות מאז שהטופס צומצם. אם יחזור — למסך העריכה של המשימה, לא לטופס היצירה.
- הצעות עיצוב שלא בוצעו: **חתימת לקוח על המכשיר**, **חותמת תאריך ושעה צרובה בתמונה**.
- שכתוב שמות המודל (`child` → עובד).
- מסך הספקים/מסמכים נשאר מינימלי עד שיהיו לקוחות משתמשים.
- **סגור ולא נפתח מחדש:** שליחה אוטומטית בוואטסאפ (סעיף 2.2).
